rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{slug} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /site/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
